{% extends 'base.html.twig' %}

{% block title %}Modifier une salle de sport
{% endblock %}

{% block body %}
	<h1>Modifier une salle de sport</h1>

	{{ include('branches/_form.html.twig', {'button_label': 'Update'}) }}
	<a href="{{ path('app_clients_show', {'id': branch.client.id}) }}">back to list</a>

	{{ include('branches/_delete_form.html.twig') }}
{% endblock %}
