{% extends 'base.html.twig' %}

{% block title %}ClientsGrants{% endblock %}

{% block body %}
    <h1>ClientsGrants</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ clients_grant.id }}</td>
            </tr>
            <tr>
                <th>Perms</th>
                <td>{{ clients_grant.perms }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_clients_grants_index') }}">back to list</a>

    <a href="{{ path('app_clients_grants_edit', {'id': clients_grant.id}) }}">edit</a>

    {{ include('clients_grants/_delete_form.html.twig') }}
{% endblock %}
