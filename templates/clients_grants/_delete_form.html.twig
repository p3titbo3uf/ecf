<form method="post" action="{{ path('app_clients_grants_delete', {'id': clients_grant.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ clients_grant.id) }}">
    <button class="btn">Delete</button>
</form>
