{% extends 'base.html.twig' %}

{% block title %}Création d'un client
{% endblock %}

{% block body %}
	<section class="container">
		<div class="row">
			<div class="col">
				<h1>Création d'un client</h1>
				{{ form_start(form) }}
				{{ form_row(form.secret) }}
				{{ form_row(form.name) }}
				{{ form_row(form.active) }}
				{{ form_row(form.short_description) }}
				{{ form_row(form.full_description) }}
				{{ form_row(form.logo_url) }}
				{{ form_row(form.url) }}
				{{ form_row(form.dpo) }}
				{{ form_row(form.technical_contact) }}
				{{ form_row(form.commercial_contact) }}
				{# {{ form_row(form.branches) }} #}
				{# <ul id="clients_form_branches___name___active" data-prototype="{{ form_widget(form.branches.vars.prototype)|e }}" data-widget-tags="{{ '<li></li>'|e }}" data-widget-counter="{{ form.branches|length }}">
																				
																									{% for branche in form.branches %}
																										<li>
																											{{ form_errors(branche) }}
																											{{ form_widget(branche) }}
																										</li>
																									{% endfor %}
																								</ul> #}
				{# {{ form_widget(form.branches.vars.prototype) }} #}
					<button type="submit" class="btn my-3"> Créer un nouveau client</button>
				{{ form_end(form) }}
				<a href="{{ path('app_security_clientlist') }}">retour à la liste des clients</a>
			</div>
		</div>
	</section>
{% endblock %}
