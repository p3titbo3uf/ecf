{% extends 'base.html.twig' %}

{% block title %}Liste des clients
{% endblock %}
{% block javascripts %}
	<script type="text/javascript"></script>
{% endblock %}
{% block body %}
	<style></style>

	<div class="container-fluid">
		<h1>Liste des clients</h1>
		<div class='row'>
			{% for c in clients %}
				{# {{ dump(c.switchActive) }} #}
				<div class='col-12 col-md-6 border rounded p2'>
					<div class="row m-1">
						<div class="col-5">{{ c.logoUrl }}
						</div>
						<div class="col-7">
							<p>{{ c.id }}</p>
							<p>{{ c.name }}</p>
							<p>{{ c.shortDescription }}</p>
							<p>{{ c.url }}</p>
						</div>
					</div>
					<div class='row m-1'>
						<a href="{{ path('app_clients_edit', {'id': c.id}) }}">edit</a>
						{# {% if c.active == true %}
													<div class="form-check form-switch">
														<input class="form-check-input" onclick="" role="switch" id="flexSwitchCheck{{c.id}}" checked>
														<label class="form-check-label" for="flexSwitchCheck{{c.id}}">Actif ?</label>
													</div>
												{% else %}
													<div class="form-check form-switch">
														<input class="form-check-input" type="checkbox" role="switch" id="flexSwitchCheck{{c.id}}">
														<label class="form-check-label" for="flexSwitchCheck{{c.id}}">Actif ?</label>
													</div>
												{% endif %} #}
					</div>
				</div>
			{% endfor %}
		</div>
	</div>
{% endblock %}
